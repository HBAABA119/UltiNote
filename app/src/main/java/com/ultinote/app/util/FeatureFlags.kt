package com.ultinote.app.util

/**
 * Kill-switches for big unfinished areas. AI companion ships in code but stays
 * fully invisible until the Gemini backend lands — one flag flips it all on.
 */
object FeatureFlags {
    const val AI_COMPANION_ENABLED = false
}
